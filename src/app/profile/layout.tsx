export const metadata = {
  title: 'My Profile | Star E-commerce',
  description: 'Manage your account settings, view order history, and update personal information.',
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
